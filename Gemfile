source "https://rubygems.org"

gem "fastlane"

# The following are only added due to warnings emitted by fastlane about dependencies using these gems as part of the default set of gems, even though they will be removed from the default set soon. So adding them here will keep things working if that removal happens and the fastlane dependencies are not updated
gem "mutex_m"
gem "abbrev"
